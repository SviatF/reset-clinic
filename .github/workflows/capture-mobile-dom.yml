name: RESET localize CSS assets

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'CSS assets already localized in migration-next-ssr.'
